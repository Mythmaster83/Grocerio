org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# permission_handler_android requires compileSdk 37; AGP 9.0.1's documented
# max is 36. Suppress the unsupported-SDK warning so the build can proceed —
# compileSdk is backward-compatible for library metadata checks.
android.suppressUnsupportedCompileSdk=37
